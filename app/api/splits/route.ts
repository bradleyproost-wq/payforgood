import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/db';export async function POST(req:Request){const u=await requireUser();const b=await req.json();await prisma.split.create({data:{userId:u.id,planName:b.planName||'Split',amount:Number(b.amount||0),items:b.items||[],status:b.status||'pending'}});return NextResponse.json({ok:true})}
