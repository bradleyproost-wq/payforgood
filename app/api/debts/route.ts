import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/db';export async function POST(req:Request){const u=await requireUser();const b=await req.json();await prisma.debt.create({data:{userId:u.id,name:b.name||'Debt',total:Number(b.total||0),paid:Number(b.paid||0),minPayment:Number(b.minPayment||0)}});return NextResponse.json({ok:true})}
