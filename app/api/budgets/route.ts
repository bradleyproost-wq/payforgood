import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/db';export async function POST(req:Request){const u=await requireUser();const b=await req.json();await prisma.budget.create({data:{userId:u.id,month:b.month||new Date().toISOString().slice(0,7),category:b.category||'ทั่วไป',limit:Number(b.limit||0),spent:Number(b.spent||0)}});return NextResponse.json({ok:true})}
