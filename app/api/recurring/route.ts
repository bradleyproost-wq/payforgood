import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/db';export async function POST(req:Request){const u=await requireUser();const b=await req.json();await prisma.recurring.create({data:{userId:u.id,name:b.name||'Recurring',kind:b.kind||'income',amount:Number(b.amount||0),frequency:b.frequency||'monthly',day:b.day?Number(b.day):null}});return NextResponse.json({ok:true})}
