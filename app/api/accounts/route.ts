import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/db';export async function POST(req:Request){const u=await requireUser();const b=await req.json();await prisma.account.create({data:{userId:u.id,name:b.name||'Account',balance:Number(b.balance||0),qrUrl:b.qrUrl||null,type:b.type||'bank'}});return NextResponse.json({ok:true})}
