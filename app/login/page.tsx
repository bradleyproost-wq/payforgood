'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter();const [mode,setMode]=useState<'login'|'register'>('login');const [form,setForm]=useState({name:'',email:'',password:''});async function submit(){const res=await fetch('/api/auth/'+mode,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(res.ok)r.push('/dashboard');else alert((await res.json()).error||'error')}return <div className="wrap" style={{maxWidth:460}}><div className="card"><h1>{mode==='login'?'Login':'สมัครสมาชิก'}</h1>{mode==='register'&&<><label>ชื่อ</label><input onChange={e=>setForm({...form,name:e.target.value})}/></>}<label>Email</label><input onChange={e=>setForm({...form,email:e.target.value})}/><label>Password</label><input type="password" onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn" onClick={submit}>{mode==='login'?'Login':'Create account'}</button><button className="btn2" style={{marginLeft:8}} onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'สมัครใหม่':'มีบัญชีแล้ว'}</button></div></div>}
